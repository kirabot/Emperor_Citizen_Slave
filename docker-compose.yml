
services:
  api:
    build: ./backend
    environment:
      NODE_ENV: production
      PORT: 8080
      FRONTEND_ORIGIN: "*"
    ports:
      - "8080:8080"
  web:
    build:
      context: ./frontend
      args:
        VITE_BACKEND_URL: "http://localhost:8080"
    ports:
      - "5173:80"
